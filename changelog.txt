Changelog for V30.0 ID(45)

Fix MISL exploding early.
Fix custom theme sliders not turning off PULSE theme automatically.
Pulse theme can now use the values from alpha slider.
Move top bar options into new theme control centre.

todo:
Fix/ change PINV activation mechanism.
New CWIR mode that makes it behave as INST (tmp = 9).
Complete auto stamp.