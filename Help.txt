//To future self, use this as a quick reference in case something goes wrong :P 
1)Icon and intro text ( just copy paste)

2)Scripts:
Copy script manager in eventcompat.lua
Add crackerk.lua to src>lua>luascripts
add same settings for crackerk.lua as eventcompat.lua in TPTscriptinterface. 

4) TOOLS:
Copy RSET.cpp into src>simulation>simtools
Add ID in meson.build

5) Dark theme:
Copy appearence.cpp from gui>interface.

6)Gravity Modes:
gui>game>gamecontroller.cpp
gui>game>optionview.cpp
src>simulation>simulation.cpp

7) HUD:
Gameview.cpp (copy fpsinfo and other parts)

8)votes:
src>gui>search>Searchview.cpp

9)Elements:               Files Modified:
(FPTC, 197);             SPRK.cpp
(FNTC, 192);             SPRK.cpp
(CLNT, 193);
(LED, 194);                SPRK.cpp
(TIMC, 195);               SPRK.cpp
(FUEL, 196);
(DMRN, 198);             Simulation.cpp
(PINV, 199);                SPRK.cpp and simulation.cpp (check for .life!)
(WALL, 200);              Simulation.cpp (search for love, define wall element)
(COND, 201);
(QGPP, 202);
(UVRD, 203);             Simulation.cpp (Add with PROT)
(SUN, 204);                Simulation.cpp (Add check for UV to not pass through)
(TMPS, 205);
(PHOS, 206);
(CMNT, 207);
(NTRG, 208);
(PRMT, 209);
(CLUD, 210);
(BEE, 211);
(ECLR, 212);             SPRK.cpp
(C16, 213);
(LITH2,214), SPRK.cpp
(PROJ, 215); 
(PPTI, 216);     SPRK.cpp
(PPTO, 217);     SPRK.cpp
(SEED, 218);    
(CSNS, 219);    
(CWIR, 220);
(CLRC, 221);  
(COPR, 222); 
(CEXP, 223); 

10) WALLS:
ZHOLE (19), Simulationdata.cpp, simulationdata.h, simulation.cpp and src>gui>graphics>Renderer.cpp (Place along with Ehole in all files.)
OWALL (20)

11) Run script update.bat to update the scripts for linux and mac.

12) To cherry pick
a) Run cherrypick.bat
b) FIlter branch to upstream> cherrypick and commit.
c) Done. 

13) To make a new Pull Request:
a) Run new branch.bat
b) Open gitbash> git checkout -b vanilla (in case it fails to checkout)
c) git checkout -b <branch name here>
d) Push commits> push to origin> Done. 

Help:
for debug, just run debug compile.bat

manual:
Debug
cd /d C:\Newmod 
cd The-Powder-Toy 
meson build-debug 
cd build-debug 
ninja

for static
cd /d C:\Newmod 
cd The-Powder-Toy (ignore if error)
meson -Dbuildtype=release -Dstatic=prebuilt -Db_vscrt=static_from_buildtype build-release-static
cd build-release-static
ninja

Deleting the tag:
git push --delete origin mytagnamehere
git tag --delete mytagnamehere